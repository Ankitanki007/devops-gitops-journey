# Write your TIL notes (replace with your real learnings): cat > projects/01-linux-git/docs/learnings.md <<'EOF' # Today I Learned — Project 01 ## Linux Permissions - chmod 600 = only owner can read/write (SSH keys, secrets) - chmod 755 = owner can do anything, others can read+execute (scripts) - The number system: r=4, w=2, x=1 — add them up per group ## SSH Insight - Private key = your identity document (never share, 600 perms) - Public key = your name badge (safe to share everywhere) - ssh-agent holds your key in memory so you don't retype passphrase ## Git Workflow - NEVER push directly to main - Branch naming: feat/, fix/, chore/, docs/ - Conventional commits make git log readable and enable automation ## Commands I'll Never Forget - ss -tlnp: see what's listening on which port - df -h / du -sh: disk usage - ps aux | grep X: find a process EOF # Final push: git checkout -b docs/project-01-complete git add . git commit -m "docs(project-01): add complete linux lab and learnings" git push origin docs/project-01-complete # → GitHub: Open PR → Merge → ✅ Project 01 Done!